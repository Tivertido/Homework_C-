// Напишите программу, которая принимает на вход трёхзначное число и на выходе показывает вторую цифру этого числа. (Проверка на трехзначность при вводе пользователем)
Console.Write("Введите трёхзначное число: ");
        string input = Console.ReadLine();

        // Проверка на трехзначность
        if (input.Length != 3) {
            Console.WriteLine("Ошибка! Введено не трёхзначное число");
            return;
        }

        // Вторая цифра - это символ с индексом 1 в строке
        char secondDigit = input[1];

        Console.WriteLine("Вторая цифра: " + secondDigit);